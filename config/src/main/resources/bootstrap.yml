server:
  port: 8000
  address:

spring:
  application:
    name: oes-config
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        instance-id: ${spring.application.name}:${random.value}
    config:
      label: master
      server:
        git:
          uri: https://github.com/housirvip/oes
          search-paths: config/src/main/resources
          username: housirvip
          password: sKUBJzXxiMv3
